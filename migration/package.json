{
  "name": "migration",
  "version": "1.0.0",
  "description": "Migration from a csv file into a query to setup the data",
  "main": "app.ts",
  "scripts": {
    "start": "npm run build && npm run execute",
    "build": "tsc --project ./tsconfig.build.json",
    "execute": "node  --experimental-modules  ./bin/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "author": "Tarik Eshaq & Jason Fan",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^13.9.8",
    "csv-parser": "^2.3.2"
  }
}
